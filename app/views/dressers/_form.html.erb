<%= form_for(@dresser) do |f| %>
  <% if @dresser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dresser.errors.count, "error") %> prohibited this dresser from being saved:</h2>

      <ul>
      <% @dresser.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :article_id %><br />
    <%= f.number_field :article_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
